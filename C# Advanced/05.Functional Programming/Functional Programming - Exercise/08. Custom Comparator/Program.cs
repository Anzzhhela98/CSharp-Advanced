using System;
using System.Linq;

namespace _08._Custom_Comparator
{
    class Program
    {
        static void Main(string[] args)
        {
            int end = int.Parse(Console.ReadLine());

            int[] divider = Console
                          .ReadLine()
                          .Split()
                          .Select(int.Parse)
                          .ToArray();
            bool isDivided = true;

        }

    }
}
